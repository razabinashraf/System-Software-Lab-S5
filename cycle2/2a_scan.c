#include <stdio.h>
#include <stdlib.h>

void main() {
    int head,wl[50],i,current,totaltracks;
    printf("\nEnter head");
    scanf("%d",&head);
    current = head;
    printf("\nEnter no of tracks to scan:");
    scanf("%d",&totaltracks);
    printf("\nEnter the tracks:");
    for (i=0;i<totaltracks; i++)
        scanf("%d",&wl[i]);
    for (i=0;i<totaltracks; i++) {

    }
}